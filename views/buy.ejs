<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buy <%= product.title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-800 text-white">
    <!-- Navbar -->
    <div class="w-100 text-white flex p-4 items-center">
        <a href="/" class="m-1 flex-grow text-center text-yellow-300 text-3xl drop-shadow-[2px_2px_2px_rgba(255,0,0,1)]">
            UniHub
        </a>
        <% if (name) { %>
            <span class="pr-2 text-white text-xl drop-shadow-[2px_2px_2px_rgba(255,255,255,0.5)] cursor-pointer">
                Hi, 
                <span class="text-yellow-300 drop-shadow-[1px_1px_1px_rgba(255,255,0,1)]">
                    <%= name %>
                </span>
            </span>
        <% } %>
    </div>

    <!-- Buy Confirmation -->
    <div class="p-8">
        <h1 class="text-4xl font-bold mb-4">Buy <%= product.title %></h1>
        <p class="text-2xl text-yellow-400 mb-4">Price: ₹<%= product.price %></p>
        <p class="text-xl mb-4">Description: <%= product.description %></p>
        <form action="/complete-purchase" method="POST">
            <input type="hidden" name="productId" value="<%= product.id %>">
            <button class="bg-green-500 text-white p-2 rounded">
                Confirm Purchase
            </button>
        </form>
    </div>
</body>
</html>
