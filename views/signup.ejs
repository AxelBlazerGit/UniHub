<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/index.css">
    <style>
        .error-message {
            color: #f20a3c; /* Tailwind's red-600 color */
            font-size: 0.875rem; /* Text-sm */
            display: none;
            text-align: center;
            margin-top: 8px;
        }
        .bg-image {
            background-image: url("/images/loginBG.png");
            background-size: cover;
            background-position: center;
        }
        .glass-effect {
            background: rgba(255, 255, 255, 0.2);
            border-radius: 16px;
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(5px);
            -webkit-backdrop-filter: blur(5px);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }
        .glass-input {
    background: rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    padding: 0.5rem;
    outline: none;
    color: #000;
    box-shadow: inset 0 1px 4px rgba(255, 255, 255, 0.3);
    backdrop-filter: blur(5px);
    -webkit-backdrop-filter: blur(5px);
    border: 1px solid rgba(255, 255, 255, 0.3);
    width: 100%;
    /* Ensure the background does not change on focus */
}

.glass-input::placeholder {
    color: rgba(0, 0, 0, 0.6); /* Adjust the opacity for better visibility */
}

/* Optional: Adjust the input field background color on focus for better UX */
.glass-input:focus {
    border-color: rgba(255, 255, 255, 0.5);
}
        .form-container {
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        .error-container {
            display: flex;
            justify-content: center;
            margin-bottom: 8px;
        }
    </style>
</head>
<body class="bg-image flex justify-center items-center h-screen">
    <div class="w-1/3 p-8 glass-effect rounded-lg shadow-lg">
        <h1 class="text-3xl text-center mb-4">Sign Up</h1>
        <form action="/signup" method="POST" class="space-y-4">
            <div>
                <input 
                    type="text" 
                    name="name" 
                    placeholder="Full Name" 
                    required 
                    class="glass-input"
                />
            </div>
            <div>
                <input 
                    type="email" 
                    name="email" 
                    placeholder="Email" 
                    required 
                    class="glass-input"
                />
            </div>
            <div>
                <input 
                    type="text" 
                    name="city" 
                    placeholder="City" 
                    required 
                    class="glass-input"
                />
            </div>
            <div>
                <input 
                    type="text" 
                    name="institution" 
                    placeholder="Institution" 
                    required 
                    class="glass-input"
                />
            </div>
            <div>
                <input 
                    type="password" 
                    name="password" 
                    placeholder="Password" 
                    required 
                    minlength="8"
                    class="glass-input"
                />
            </div>
            <button type="submit" class="w-full p-2 bg-blue-500 text-white rounded">Sign Up</button>
        </form>
        <a href="/login" class="block text-center mt-4 text-blue-500 hover:underline">Already have an account? Log in</a>
    </div>
</body>
</html>
