<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Purchase Complete</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-800 text-white flex items-center justify-center h-screen">
    <div class="bg-zinc-700 p-8 rounded-lg shadow-lg text-center">
        <h1 class="text-3xl font-bold text-yellow-300 mb-4">Purchase Complete</h1>
        <p class="text-xl mb-6">Thank you for your purchase! Your order has been successfully placed.</p>
        
        <!-- Display purchased item details -->
        <div class="bg-zinc-800 p-4 rounded-lg shadow-md mb-6 flex items-center justify-center">
            <img src="<%= product.image %>" alt="<%= product.title %>" class="w-32 h-32 object-cover rounded-lg mr-4"/>
            <div class="text-left">
                <h3 class="text-2xl font-semibold mb-2"><%= product.title %></h3>
                <p class="mb-2"><%= product.description %></p>
                <p class="text-yellow-400 text-xl font-bold">₹<%= product.price %></p>
            </div>
        </div>

        <!-- Navigation buttons -->
        <div class="flex justify-center gap-4">
            <a href="/" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Home</a>
            <a href="/profile" class="bg-yellow-400 text-white px-4 py-2 rounded hover:bg-yellow-500">View Profile</a>
        </div>
    </div>
</body>
</html>
